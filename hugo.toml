baseURL = "https://kropath.github.io/kropath-docs/"
title = "kropath Documentation"
enableRobotsTXT = true
enableGitInfo = true
refLinksErrorLevel = "ERROR"

# Docsy needs the extended (SCSS-enabled) Hugo binary.
[module]
  [module.hugoVersion]
    extended = true
    min = "0.146.0"
  [[module.imports]]
    path = "github.com/google/docsy/theme"

[languages]
  [languages.en]
    label = "English"
    locale = "en-us"
    contentDir = "content/en"
    weight = 1

[params]
  copyright = "kropath contributors"
  github_repo = "https://github.com/kropath/kropath-docs"
  github_branch = "main"
  # No third-party search account — start with the offline Lunr index
  # (spec §14 open question 2).
  offlineSearch = true

[params.ui]
  sidebar_menu_compact = false
  sidebar_search_disable = false
  breadcrumb_disable = false
  sidebar_menu_foldable = true
  showLightDarkModeMenu = true

[params.ui.feedback]
  enable = false

# Pinned exact version (Docsy's mermaid script partial warns on anything
# else) — kept in sync with versions.env MERMAID_VERSION. Docsy's built-in
# render hook (render-codeblock-mermaid.html) turns a ```mermaid fenced code
# block into this script, loaded from jsdelivr only on pages that use one.
[params.mermaid]
  version = "12.0.0"

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
  [markup.tableOfContents]
    startLevel = 2
    endLevel = 4

[outputs]
  home = ["HTML", "RSS"]
  section = ["HTML", "RSS"]

[security]
  [security.funcs]
    getenv = ["^HUGO_", "^CI$"]
