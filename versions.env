# Single source of truth for all pinned tool versions used by the Makefile and
# .github/workflows/docs.yml. Do not duplicate a version literal anywhere else.
HUGO_VERSION=0.166.0
DOCSY_VERSION=v0.17.0
# Mermaid CDN version for Docsy's built-in diagram render hook (hugo.toml
# params.mermaid.version). Not read by the Makefile/CI — recorded here as
# the single source of truth per repo convention, same as DOCSY_VERSION.
MERMAID_VERSION=12.0.0
NODE_VERSION=24.21.0
GO_VERSION=1.27.1
MARKDOWNLINT_CLI2_VERSION=0.23.3
HTMLTEST_VERSION=0.17.0
PYTHON_VERSION=3.14
